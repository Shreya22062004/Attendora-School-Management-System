import React,{useState} from "react";import api from "../services/api";
export default function Login({onLogin}){const [username,setUsername]=useState(localStorage.getItem("last_username")||"admin"),[password,setPassword]=useState(""),[error,setError]=useState("");const submit=async e=>{e.preventDefault();setError("");try{const r=await api.post("/auth/login",{username,password});localStorage.setItem("school_token",r.data.access_token);localStorage.setItem("school_user",r.data.username);localStorage.setItem("school_role",r.data.role);localStorage.setItem("school_name",r.data.school_name||"");localStorage.setItem("last_username",username);onLogin(r.data.username)}catch(e){setError(e.response?.data?.detail||"Login failed")}};return <div className="login-page"><form className="login-card" onSubmit={submit}><div className="brand-mark login-mark">SA</div><h1>School Attendance</h1><p className="muted">Sign in to manage attendance records</p><label>Username<input value={username} onChange={e=>setUsername(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="alert error">{error}</div>}<button className="primary-btn" type="submit">Sign In</button><small className="muted">Initial login: admin / admin123</small></form></div>}
